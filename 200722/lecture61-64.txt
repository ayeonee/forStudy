//정적 멤버 : 전역으로 선언된 것을 클래스 안으로 넣고 싶을 때 사용
//static 붙여줌. 정적 멤버들은 클래스 안에서는 값을 대입할 수 없음.
//장점 : private 값들에도 접근할 수 있음. 전역 변수를 많이 선언하다보면 이름의 중복을 피하는 등 혼란이 옴. 전역변수나 전역함수는 잘 사용하지 않는 것이 원칙.

//상수형 매개 변수 : const (이를 사용해서 선언하면 그 값을 바꿀 수 없음)
//메서드의 상수화 : 어떤 메서드에 대해서 이 안에 속한 다른 멤버변수를 건드릴 수 없도록 막아버림. int 함수이름() const{} 해줌. const를 꼭 ()안에 써주어야함. 이 안에는 바뀔 수 있는 값이 있으면 안됨.(ex. a++) 